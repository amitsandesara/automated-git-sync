# Git Sync Configuration
# This file contains customizable settings for the automated git sync scripts.

# Directory containing git repositories
CODE_DIR="$HOME/code"

# Log retention period in days (0 to disable cleanup)
LOG_RETENTION_DAYS=7

# Directories to skip during update (space-separated)
SKIP_DIRS="logs .DS_Store automated-git-sync"

# Repositories to exclude from updates (space-separated)
# These repositories will be skipped entirely during the sync process
EXCLUDED_REPOS=""

# Default branch names to check (space-separated, in order of preference)
DEFAULT_BRANCHES="main master"

# Enable verbose logging (true/false)
VERBOSE="false"

# Enable dry-run mode (true/false)
DRY_RUN="false"

# Cron schedule for primary and fallback runs
# Format: MINUTE HOUR DAY_OF_MONTH MONTH DAY_OF_WEEK
# Default primary run: 9:15 AM on weekdays (Mon-Fri)
PRIMARY_CRON_SCHEDULE="15 9 * * 1-5"

# Default fallback run: 9:30 AM on weekdays (Mon-Fri)
FALLBACK_CRON_SCHEDULE="30 9 * * 1-5"

# Custom commands for specific repositories
# Use semicolon to separate different repositories and colon to separate repo name from commands
# Uncomment and customize as needed
# REPO_COMMANDS='repo_name1:command1 && command2;repo_name2:command3 && command4'
# REPO_COMMANDS='repo_1:bundle install && bundle exec rails db:migrate;repo_2:npm install && npm run build'